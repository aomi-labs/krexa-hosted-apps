[package]
name = "hello-ci"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
aomi-sdk = "=0.1.20"
schemars = "1.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
